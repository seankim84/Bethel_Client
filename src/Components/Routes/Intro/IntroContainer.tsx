import React from 'react';
import Intro from './IntroPresenter';

class IntroContainer extends React.Component {
    public  render(){
        return(
            <Intro />
        )
    }
}

export default IntroContainer;
